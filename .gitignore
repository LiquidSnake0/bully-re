# Tout ce qui derive du binaire du jeu reste sur la machine : le depot ne
# contient que du code ecrit a la main et de la documentation.
export/*.c
export/*.log
export/fonctions.tsv
export/fonctions.jsonl
export/index.sqlite

# Sorties de compilation
build/
# sorties de outils/nif2obj : dérivées des données du jeu, jamais dans le dépôt
export-obj/
